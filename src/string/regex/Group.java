package string.regex;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Group {
    static public final String POEM = "nicew icebie iewcbiweu  iecbwi";

    public static void main(String[] args) {
        Matcher m = Pattern.compile("").matcher(POEM);
        
    }
}
